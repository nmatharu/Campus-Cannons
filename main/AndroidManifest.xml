<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="iat359.nmatharu.campuscannons">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/icon"
        android:isGame="true"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu.MenuActivity" />
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".gameplay.WorldMapActivity" />
        <activity android:name=".gameplay.BattleActivity" />
        <activity android:name=".gameplay.hubPackage.HubActivity" />
        <activity android:name=".gameplay.inventoryPackage.InventoryActivity" />
        <activity android:name=".battleIndex.BattleIndexActivity" />
        <activity android:name=".leaderboard.LeaderboardActivity"></activity>
    </application>

</manifest>